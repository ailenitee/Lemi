## Frameworks

Laravel, Bootstrap, SASS, CSS, Laravel Mix

Laravel Mix to compile SASS to CSS.

## Steps on how to run the project

  1. composer install
  2. npm install
  3. duplicate .env.example file and rename the new one to .env
  4. php artisan key:generate
